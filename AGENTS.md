# jeryu-tool-finder Agent Instructions

The **tool-discovery** arm of the Jeryu split family: scan every repo, cluster
code repeated across more than one repo, and propose the strongest clusters as
reusable tools into `jeryu-tool`.

Before editing, read `README.md` and `docs/tool-finder.md`.

This repo is **pure scripts + docs — no product code** (Python lives under
`scripts/` and `ops/`). It takes **no Cargo dependency**: the cross-repo cluster
engine lives in `jeryu-intelligence/crates/jeryu-codegraph` and is invoked as a
binary at runtime (`tool-build scan-family`). Do not add a build system or pin a
crate here — that is the whole point of keeping it split out.

The pipeline is `scan_family.py → dossier.py → propose.py`. `propose.py` MUTATES
the sibling `jeryu-tool` repo (registry entry + build task); keep its output
valid against `jeryu-tool`'s `ops/registry_summary.py --check`.

`dossiers/` is a generated zone (regenerated by `just scan && just dossier`); do
not hand-edit or commit its contents. Required local commands are `just check`,
`just score`, and `just security`; `just` runs the full gate. There is no `fast`
lane here — the jankurai pin is owned by `jeryu-tool`, not this repo.
